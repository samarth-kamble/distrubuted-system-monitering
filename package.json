{
  "name": "pulse-guard",
  "version": "1.0.0",
  "scripts": {
    "dev:api": "bun --filter api start:dev",
    "build:api": "bun --filter api build",
    "dev:worker": "bun --filter worker start:dev",
    "build:worker": "bun --filter worker build"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "dependencies": {
    "@prisma/adapter-pg": "^7.9.1",
    "@prisma/client": "^7.9.1",
    "dotenv": "^17.4.2",
    "pg": "^8.23.0"
  },
  "devDependencies": {
    "@types/pg": "^8.21.0",
    "argon2": "^0.45.1",
    "prisma": "^7.9.1"
  }
}