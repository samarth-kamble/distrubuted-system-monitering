generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Demo {
  id        String   @id @default(uuid()) @db.Uuid
  name      String
  createdAt DateTime @default(now())

  @@map("demos")
}

